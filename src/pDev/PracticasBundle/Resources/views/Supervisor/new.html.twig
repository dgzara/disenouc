{% extends 'pDevPracticasBundle::layout.html.twig' %}

{%block breadcrumb%}
{{parent()}}
                
{%endblock%}


{%block header%}Supervisor{%endblock%}
{%block lead%}Agregar nuevo supervisor {%endblock%}

{%block content %}
    <form action="{{ path('practicas_supervisor_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-primary" type="submit">Guardar</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('practicas_supervisor') }}">
            Volver
        </a>
    </li>
</ul>
{% endblock %}
