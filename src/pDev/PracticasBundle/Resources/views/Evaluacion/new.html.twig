{% extends 'pDevPracticasBundle::layout.html.twig' %}

{%block breadcrumb%}
{{parent()}}
                
{%endblock%}


{%block header%}Crear evaluación{%endblock%}
{%block lead%} {%endblock%}

{%block content %}
    <form class="well form-horizontal" action="{{ path('practicas_evaluacion_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-primary" type="submit">Crear</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('practicas_evaluacion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
