{% extends 'pDevWebBundle::layout.html.twig' %}
{% trans_default_domain "FOSUserBundle" %}


{%block breadcrumb%}
{{parent()}}
                <li class="active"><span class="divider">/</span><a href="{{ path('fos_user_security_login') }}">Ingresar</a></li>
{%endblock%}
                
{%block navleft%}
{%endblock%}

{%block header%}Solicitud de acceso{%endblock%}
{%block lead%}Ingrese sus datos{%endblock%}
{%block content %}
{% trans_default_domain 'FOSUserBundle' %}

<form class="form-horizontal form-small form-well" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <p class="pull-right">
        <input class="btn btn-primary" type="submit" value="{{ 'registration.submit'|trans }}" />
    </p>
    <div class="clearfix"></div>
</form>

{%endblock%}